name: Cloudflare WAF Validation with OPA

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

env:
  TF_CLOUD_ORGANIZATION: "infra-aymeric-cfd"
  TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}

jobs:
  validate:
    name: Validate and Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    outputs:
      requires_approval: ${{ steps.override_check.outputs.requires_approval }}
      run_id: ${{ steps.plan.outputs.run_id }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
          terraform_version: "1.6.0"

      - name: Setup OPA
        uses: open-policy-agent/setup-opa@v2
        with:
          version: latest

      - name: Terraform Format Check
        id: fmt
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color

      - name: Run OPA Tests
        id: opa_test
        run: |
          echo "Running OPA policy tests..."
          opa test policies/ -v
          echo "✅ OPA tests passed"

      - name: Terraform Plan
        id: plan
        run: |
          PLAN_OUTPUT=$(terraform plan -no-color 2>&1)
          echo "$PLAN_OUTPUT"

          RUN_ID=$(echo "$PLAN_OUTPUT" | grep -o 'run-[A-Za-z0-9]*' | head -1)
          echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
          echo "Captured TFC Run ID: $RUN_ID"
        continue-on-error: true

      - name: Download Plan JSON from Terraform Cloud
        id: download_plan
        run: |
          RUN_ID="${{ steps.plan.outputs.run_id }}"
          if [ -z "$RUN_ID" ]; then
            echo "⚠️  No run ID captured, skipping plan JSON download"
            exit 0
          fi

          RUN_DATA=$(curl -s \
            -H "Authorization: Bearer $TF_API_TOKEN" \
            -H "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/runs/$RUN_ID")

          PLAN_ID=$(echo "$RUN_DATA" | jq -r '.data.relationships.plan.data.id')
          PLAN_STATUS=$(echo "$RUN_DATA" | jq -r '.data.attributes.status')
          echo "Run status: $PLAN_STATUS"
          echo "Plan ID: $PLAN_ID"

          # Wait for plan to reach a finished state if still in progress
          for i in $(seq 1 10); do
            PLAN_STATUS=$(curl -s \
              -H "Authorization: Bearer $TF_API_TOKEN" \
              "https://app.terraform.io/api/v2/plans/$PLAN_ID" \
              | jq -r '.data.attributes.status')
            echo "Plan status (attempt $i): $PLAN_STATUS"
            [ "$PLAN_STATUS" = "finished" ] && break
            sleep 5
          done

          HTTP_STATUS=$(curl -s -o tfplan.json -w "%{http_code}" \
            -H "Authorization: Bearer $TF_API_TOKEN" \
            "https://app.terraform.io/api/v2/plans/$PLAN_ID/json-output")

          echo "HTTP status for json-output: $HTTP_STATUS"
          [ -s tfplan.json ] && echo "✅ Plan JSON downloaded" || echo "⚠️  Plan JSON empty or unavailable"
        continue-on-error: true

      - name: Check for Managed Ruleset Overrides
        id: override_check
        run: |
          if [ ! -f tfplan.json ]; then
            echo "requires_approval=false" >> $GITHUB_OUTPUT
            echo "⚠️  No plan file found, skipping override check"
            exit 0
          fi
          APPROVAL_OUTPUT=$(opa eval -i tfplan.json -d policies/waf_required.rego "data.terraform.requires_approval" --format pretty)
          if echo "$APPROVAL_OUTPUT" | grep -q "APPROVAL REQUIRED"; then
            echo "requires_approval=true" >> $GITHUB_OUTPUT
            echo "⚠️  Managed ruleset overrides detected - additional approval will be required before apply"
          else
            echo "requires_approval=false" >> $GITHUB_OUTPUT
            echo "✅ No managed ruleset overrides detected"
          fi

      - name: Validate Plan with OPA
        id: opa_eval
        run: |
          echo "Evaluating Terraform plan against OPA policies..."
          
          # Check for policy violations (deny rules)
          DENY_OUTPUT=$(opa eval -i tfplan.json -d policies/waf_required.rego "data.terraform.deny" --format pretty)
          echo "Deny evaluation result:"
          echo "$DENY_OUTPUT"
          
          # Check for warnings
          WARN_OUTPUT=$(opa eval -i tfplan.json -d policies/waf_required.rego "data.terraform.warn" --format pretty)
          echo "Warning evaluation result:"
          echo "$WARN_OUTPUT"
          
          # If deny output contains violations, fail the check
          if echo "$DENY_OUTPUT" | grep -q "POLICY VIOLATION"; then
            echo "❌ OPA policy violations found!"
            echo "$DENY_OUTPUT"
            exit 1
          fi
          
          echo "✅ OPA policy validation passed"
          
          # Display warnings if any
          if echo "$WARN_OUTPUT" | grep -q "WARNING"; then
            echo "⚠️  Warnings found:"
            echo "$WARN_OUTPUT"
          fi

      - name: Post Plan to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          PLAN: "${{ steps.plan.outputs.stdout }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
            #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
            #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
            #### OPA Policy Tests 📋\`${{ steps.opa_test.outcome }}\`
            #### OPA Policy Validation 🔒\`${{ steps.opa_eval.outcome }}\`
            #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`terraform
            ${process.env.PLAN}
            \`\`\`

            </details>

            **Pusher:** @${{ github.actor }}
            **Action:** ${{ github.event_name }}
            **Workflow:** ${{ github.workflow }}`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

  security-review-override:
    name: Security Review - Managed Ruleset Override
    needs: validate
    if: needs.validate.outputs.requires_approval == 'true' && github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    environment: managed-ruleset-override
    steps:
      - name: Override Approved
        run: echo "✅ Managed ruleset override reviewed and approved"

  apply:
    name: Terraform Apply
    needs: validate
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read

    steps:
      - name: Confirm and Apply TFC Run
        run: |
          RUN_ID="${{ needs.validate.outputs.run_id }}"
          echo "Applying TFC run: $RUN_ID"

          curl -s \
            -X POST \
            -H "Authorization: Bearer $TF_API_TOKEN" \
            -H "Content-Type: application/vnd.api+json" \
            -d '{"data":{"attributes":{"comment":"Approved via GitHub Actions"},"type":"runs"}}' \
            "https://app.terraform.io/api/v2/runs/$RUN_ID/actions/apply"

          echo "✅ Apply confirmed on Terraform Cloud — monitor progress at:"
          echo "https://app.terraform.io/app/$TF_CLOUD_ORGANIZATION/workspaces"
